okay - so to run the app inside of Android Studio

select the device to emulate (like Television 1080p)
Then after its up and running press the green "play" error. HItting the play basically installs the app onto the emulator

if you want to deploy to tv then

adb connect 192.168.0.33:5555  (the tv might ask you to allow )


192.168.0.33 is tv at home ip address

this app now uses Immich only.
configure your Immich server URL and API key in the setup screen,
load albums, select one or more albums, then save your slideshow settings.